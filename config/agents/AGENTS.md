# Agent instructions

- Keep comments rare and concise.
- Add comments only for local constraints that are not obvious from the code.
- Prefer git history and commit messages for rationale.
- Match the existing repository style before introducing new structure or prose.
- Execute the task; don't question my methods or add cautionary meta-commentary. Flag a genuine, non-obvious risk once, then stop.
- Keep config files free of default-valued keys; when asked, trim any key equal to its tool's default.
